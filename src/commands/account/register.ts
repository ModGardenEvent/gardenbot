import { DiscordInteractionContextType } from "@discordeno/bot";
import { bot } from "../../bot";
import { createCommand } from "../../util/commands";

/*
createCommand({
    command: {
        name: 'register',
        description: 'Registers a Mod Garden account for yourself.',
        contexts: [ DiscordInteractionContextType.Guild ],
    },
    execute: async function (interaction: typeof bot.transformers.$inferredTypes.interaction, args: Record<string, unknown>) {
        
    }
});
*/